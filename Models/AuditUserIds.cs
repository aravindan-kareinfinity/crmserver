namespace CRM.Server.Models
{
    /// <summary>Default <c>users.id</c> for system / unauthenticated audit rows when no actor is known.</summary>
    public static class AuditUserIds
    {
        public const long System = 1;
    }
}
